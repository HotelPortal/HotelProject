@model IEnumerable<HotelProject.Models.status_quarto>

@{
    ViewBag.Title = "Status Quarto";
    Layout = "~/Views/Shared/_BootstrapLayout.basic.cshtml";
}

<h2>Status</h2>

<p>
    @Html.ActionLink("Novo Status +", "Create")
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th>
            #
        </th>
        <th>
            Descrição
        </th>
        <th>
            Alugável
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.status_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descridao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FlAlugavel)
        </td>
        <td>
            @*@Html.ActionLink("Editar", "Edit", new { id=item.status_Id }) |
            @Html.ActionLink("Detalhar", "Details", new { id=item.status_Id }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.status_Id })*@
            
            <div class="btn-group">
                    <a class="btn dropdown-toggle btn-inverse" data-toggle="dropdown" href="#">
                        Ação
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>@Html.ActionLink("Editar", "Edit", new {  id=item.status_Id })</li>
                        <li>@Html.ActionLink("Detalhar", "Details", new { id=item.status_Id  })</li>
                        <li class="divider"></li>
                        <li>@Html.ActionLink("Deletar", "Delete", new {  id=item.status_Id })</li>
                    </ul>
             </div>
        </td>
    </tr>
}

</table>
